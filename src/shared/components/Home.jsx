import React from 'react';
import Header from './Header';

export default React.createClass({
    render: function(){
        return (
            <div className="home">
                <Header />
            </div>
        );
    }
});
